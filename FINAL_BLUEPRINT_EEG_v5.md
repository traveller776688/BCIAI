# DAME：脑区联动耦合驱动的跨被试情感解码 — 互助型神经元社会架构

## 最终版实验报告 — 完整架构 · 定理 · 实验数据（EEG版 v5，SEED-IV 原始波形 · 跨被试 LOSO + 跨会话双协议）

---

# 第一部分：问题定义与跨被试迁移的形式化

### 1.1 核心问题

**跨被试情感解码**：从脑电信号识别情绪状态，且识别能力不受被试个体差异影响。

- 迁移目标 = **被试之间**（Leave-One-Subject-Out, LOSO），单数据集 SEED-IV；
- 不做跨数据集适配：被试个体差异本身就是需要被"预迁移"机制学掉的域；
- 直接迁移协议下，模型只在源被试上训练，测试被试**完全未见**；
- **第二协议 = 同脑跨会话**（train s1+s2 → test s3）：与 LOSO 构成"大域差 vs 小域差"对照，是 5.6/5.7 场域条件化机制定律的实验装置。

### 1.2 核心前提：脑区的联动耦合

情感状态不是单个电极的活动，而是**大尺度脑功能网络的联动重组**——默认模式网络（DMN，前枢纽前额叶 PFC ↔ 后枢纽后扣带/楔前叶 CP）在情绪加工中显著参与，正性情绪下 θ/α 段 DMN 内相位同步增强（Bai et al., 2025, EMBC）。

**本工作的探测单元是 12 个脑区之间的相位耦合（PLV），不是 62 个电极节点。** 该前提已被消融实验三轮反复验证（见 5.1：去掉耦合 −3.3 / −3.5 / −3.7 点，唯一三轮连硬的机制）。

### 1.3 为什么必须用原始波形

DE（差分熵）特征只含幅度/能量信息，**没有相位**，无法计算 PLV 相位耦合。因此本工作使用 SEED-IV **原始波形**（800 Hz → 降采样 200 Hz），在时域保留相位结构，为耦合计算提供物理基础。

### 1.4 形式化设定

设被试集 $\mathcal{S} = \{1,\dots,15\}$，每被试含 3 个 session × 24 个 trial，情绪标签 $y \in \{0,1,2,3\}$（中性/悲伤/恐惧/快乐）。原始波形按 4 s 窗口、2 s 步长切片，共约 20K 窗口：

$$(X, y) \in \mathbb{R}^{N \times 62 \times 800}, \quad y \in \{0,\dots,3\}$$

LOSO：对被试 $s$，训练集 $\mathcal{D}_{train} = \{(X_i, y_i): s_i \neq s\}$，测试集 $\mathcal{D}_{test} = \{(X_i, y_i): s_i = s\}$。评价指标 = 15 折准确率均值 ± 标准差，多种子（42/123/789）配对显著性检验（见 5.2.3）。

跨会话协议：对被试 $s$，训练集 = session 1+2 窗口，测试集 = session 3 窗口，全体被试池化取均值；两协议共用同一批窗口与预处理，仅划分不同（数据零成本）。

---

# 第二部分：DAME-C5 架构（五阶段）

## 2.1 信号处理前端：脑区相位耦合表示

**62 通道 → 12 脑区**（PFC / FL / FR / FC / C / TL / TR / CP / P / PO / O / CB，按 10-20 系统分组，区内取均值）→ **5 频段 FFT 带通**（δ 1–4 / θ 4–8 / α 8–13 / β 13–30 / γ 30–45 Hz）→ **Hilbert 相位** → **PLV 耦合矩阵**。

对脑区对 $(i,j)$、0.5 s 子窗（每窗口 8 步）内的相位差，PLV 定义为相位一致性的向量平均模：

$$PLV_{ij} = \left| \frac{1}{T_c} \sum_{t} e^{\,i\,(\phi_i(t)-\phi_j(t))} \right| \in [0,1]$$

实现上以余弦/正弦均值合成（全可微，端到端梯度可流入相位）：

$$PLV_{ij} = \sqrt{\big(\langle \cos\Delta\phi\rangle\big)^2 + \big(\langle \sin\Delta\phi\rangle\big)^2}$$

产出双支路表示：

- **耦合支路** $H_{coup} \in \mathbb{R}^{B \times 8 \times 256}$：5 频段 × 66 脑区对的 PLV token 序列——水循环的蒸馏对象；
- **活动支路** $H_{pow} \in \mathbb{R}^{B \times 8 \times 64}$：5 频段 × 12 脑区的带内功率——社会的原始门控特征与分类残差。

PLV 在**所有消融臂中始终计算**（保证 NoCoupling 等消融的可比性与诊断一致性）。

## 2.2 水循环（WaterCycleV2）：蒸馏耦合本征

水循环对**耦合 token 序列**执行三重操作（蒸馏去域）：

**（1）蒸发 — 变分信息瓶颈（VIB）**：把 256 维耦合 token 压缩到 $K=32$ 维本征

$$Z \sim q_\phi(Z \mid \bar{H}_{coup}) = \mathcal{N}\big(\mu_\phi(\bar{H}_{coup}),\, \sigma_\phi^2(\bar{H}_{coup})\big), \qquad \mathcal{L}_{KL} = KL\big(q_\phi \,\|\, \mathcal{N}(0, I)\big)$$

**（2）降雨 — 交叉注意力软匹配**：本征 $Z$ 反哺耦合 token 序列，注意力加权得到锚点

$$A = \sum_k \alpha_k \cdot V_k, \qquad \alpha = \mathrm{softmax}\!\left(\frac{Z^\top W_q \cdot H_k W_k}{\sqrt{d}}\right)$$

**（3）回流 — Banach 不动点迭代（隐式模型）**：蒸馏器反复精炼本征直至收敛

$$Z^{(t+1)} = \mu\big(R + g_\varphi(Z^{(t)})\big), \qquad Z^* = \lim_{t\to\infty} Z^{(t)}$$

**定理层级（收敛保证）：**

> **Lemma 1.** 谱归一化合成网络满足 Lipschitz 常数 ≤ 1。
> **Lemma 2.** 线性映射 $W_\mu$ 无谱归一化时，$L_\mu = \sigma_{max}(W_\mu)$。
> **Lemma 3.** $g_\varphi$（两层谱归一化 MLP）满足 $L_g \le s_{reflux}$。
> **Theorem 1（收缩映射条件）.** $T(Z) = \mu(R + g_\varphi(Z))$ 是 Banach 收缩映射 $\iff \gamma = L_\mu \cdot L_g < 1$。
> **Theorem 2（指数收敛）.** 当 $\gamma < 1$，$\|Z^{(t)} - Z^*\| \le \gamma^t \|Z^{(0)} - Z^*\|$，实测收敛 2–4 步。

**与 DEQ / 隐式模型的关系**：回流就是深度平衡模型（Deep Equilibrium, Bai et al. 2019）的显式迭代形态——$Z^*$ 是隐式方程 $Z = T(Z)$ 的不动点。区别在于：DEQ 求解的是通用平衡点，而我们用**谱范数显式构造收缩算子**，获得良定性（存在唯一性）+ 收敛速率可证（Theorem 2），并用显式迭代避免隐函数求导，梯度经不动点的传播语义与隐式微分（implicit differentiation）一致。这一对比构成理论贡献的一层。

## 2.3 互助神经元社会（MutualSocietyV3）：边锚定脑区专家协作（v5_modedge 定稿）

$N = 12$ 个专家与 12 个脑区一一对应，构成非对抗的协作社会：

**（1）结构初始化来自实测耦合**：互助矩阵 $W_{mutual} \in \mathbb{R}^{N \times N}$ 用**训练集上实测的 PLV 邻接**初始化——谁的耦合强谁互助，结构来自数据而非随机（机制深度绑定耦合的证据）。

**（2）专家注视入射耦合边**：专家身份 = 脑区策略锚（如如不动），注视对象 = 该脑区的 **11 条入射 PLV 边 × 5 频段模式**（`_inc_idx` 结构索引）——社会注视的对象是耦合边本身，与"脑区联动耦合"基座哲学对齐（v5_modedge：对齐基座后贡献仍未转正，问题不在注视对象，见 8.1）。

**（3）边模式门控（耦合直接参与社会决策）**：

$$g_i = \sigma\Big(\mathrm{temp} \cdot \cos\big(\text{边模式}_i,\ \text{策略}_i\big) + b_i + \mathrm{proj}_{coup}\big(\text{耦合强度快读数}\big)\Big), \qquad \mathrm{temp}: 0.8 \to 3.5 \text{ 退火}, \ b_i^{init} = -0.2$$

**（4）三股信息流门控记忆**：外部驱动（入射边专家状态）+ 社区内互助 + 自反馈，三流求和经 LayerNorm/tanh 成候选记忆，sigmoid 更新门 η 线性混合新旧记忆（$m' = (1-\eta)m + \eta\,\tilde{m}$）——GRU 风格的门控更新，无重置门，跟踪边模式跨窗口动态。

**（5）社区涌现**：按共激活聚类把 12 个脑区专家划分为 4 个功能社区，**脑区 → 功能网络的分配完全自发涌现**，可直接与神经科学大尺度网络（DMN/视觉/运动）对照（见 5.3 诊断图）。

**（6）社会输出 = 策略调制器（融合 v6）**：社会输出 $O$ 经 $\mathrm{proj}_w$ 生成各机制项的权重（2.5），不再是可加槽位（L1 教训：可加项浮在下游会被 LayerNorm 中和）。消融臂 C5-NoPredNoMutual = 均匀权重、结构相同，只删社会调制。场域路由 ω 已证伪关闭（FIELD_ROUTER=False → ω≡1，社会全权重，见 8.2）。

## 2.4 稳定性预测头（StabilityPredHead）：预迁移新定义

**预迁移 = 预测情绪状态的时序演化**，而不是预测耦合数值（平凡基线 trivCos≈0.97 证明数值预测无信息增益）。可证伪目标：

> 给定当前窗口，预测**下一个 trial 的情绪标签是否保持不变**（二分类：保持 vs 翻转，随机基线 50%，真实保持率基线 ~60–70%）。

$$\hat{s}_t = f_{stab}\big([Z_t,\, A_t]\big), \qquad s_t = \mathbb{1}[\,y_t = y_{t+1}\,]$$

三层证据设计（论文级验证）：

1. 稳定性预测准确率 vs 真实保持率基线（跨 trial 配对，同 session 内末窗 → 下一 trial 首窗）；
2. 哪类情绪稳定性可预测（与 DMN 文献对照）；
3. 预测置信度与真实翻转率的校准曲线。

**与分类器彻底解耦**：预测头不向融合层提供任何特征（iter3 教训——ĉ 进融合被分类梯度征用，selfMSE 从 0.0002 涨到 0.04，预迁移被架空；该设计已撤回）。

**Pass1 耦合自洽约束**：预测头内独立读出器从 $Z$ 重建固定正交投影的 PLV 耦合本征，$\mathcal{L}_{self} = \|\hat{c}_{self} - W_{proj}^\top PLV\|^2$，$W_{proj}$ **冻结**（不可作弊）——保证水循环蒸馏出的 $Z$ 确实保留了耦合结构（P5：蒸馏≠遗弃）。

## 2.5 分类器：融合 v6（社会策略调制权重）

$$f = \sum_i w_i \cdot \mathrm{term}_i, \qquad \mathrm{terms} = \Big\{\underbrace{\mathrm{proj}_{pool}(\bar{H}_{coup})}_{\text{耦合池化}},\ \underbrace{\mathrm{proj}_{pow}(\bar{H}_{pow})}_{\text{功率残差}},\ \underbrace{\mathrm{proj}_{anchor}(A) + \mathrm{proj}_Z(Z)}_{\text{水循环}}\Big\}$$

$$w = \mathrm{softmax}\big(\mathrm{proj}_w(O)\big)\ \ (\text{use\_mutual}), \qquad w = \tfrac{1}{n_{terms}}\mathbb{1}\ \ (\text{NoMutual 对照，结构相同})$$

$$\hat{y} = \mathrm{softmax}(W_{clf}\, f + b_{clf})$$

场域路由 ω 已证伪关闭（ω≡1，社会全权重，8.2）；预测头 ĉ 不进入融合（iter3 教训，2.4）。消融 = 删除对应项 / 换均匀权重，维度恒定——消融表干净可审计。

---

# 第三部分：完整损失函数

### 3.1 端到端训练目标

$$\mathcal{L} = \mathcal{L}_{CE} + \lambda_{KL}\,w_{kl}\,\mathcal{L}_{KL} + \lambda_O\,\mathcal{L}_{ortho} + \lambda_M\,\mathcal{L}_{mutual} + \lambda_S\,\mathcal{L}_{spec} + \lambda_E\,\mathcal{L}_{gate} + \lambda_{coup}\,\mathcal{L}_{self} + \lambda_{stab}\,w_{stab}\,\mathcal{L}_{stab} + \lambda_R\,\mathcal{L}_{reflux}$$

### 3.2 各项定义

| 项 | 定义 | 权重 |
|---|---|---|
| $\mathcal{L}_{CE}$ | 交叉熵（label smoothing 0.1） | 1 |
| $\mathcal{L}_{KL}$ | VIB 信息瓶颈（warmup 8 轮） | 0.008 |
| $\mathcal{L}_{ortho}$ | 专家原型正交性 | 1e-3 |
| $\mathcal{L}_{mutual}$ | 社区内互助一致性 | 1e-3 |
| $\mathcal{L}_{spec}$ | 专家专长多样性 | 1e-3 |
| $\mathcal{L}_{gate}$ | 门控熵（防崩溃） | 1e-3 |
| $\mathcal{L}_{self}$ | 耦合自洽：$Z$ 重建冻结投影的 PLV 本征 | 0.005 |
| $\mathcal{L}_{stab}$ | 跨 trial 情绪稳定性二分类（warmup：第 4 轮后启动） | 0.05 |
| $\mathcal{L}_{reflux}$ | 回流幅度下界：$\mathrm{ReLU}(0.01 - \|g_\varphi(Z)\|)$ | 0.01 |

### 3.3 两遍训练调度

- **Pass 1（全窗口）**：分类 + 全部机制损失 + 耦合自洽约束；
- **Pass 2（配对窗口）**：跨 trial 配对（本 trial 末窗 → 下一 trial 首窗）上计算稳定性损失，第 4 轮后 warmup 启动——前期不向分类梯度灌入稳定性目标的噪音。

---

# 第四部分：超参数表

### 4.1 信号处理

| 参数 | 值 |
|---|---|
| 采样率（降采样后） | 200 Hz |
| 窗口长度 / 步长 | 4 s (800 点) / 2 s |
| 耦合子窗 | 0.5 s（每窗口 8 步） |
| 频段 | δ 1–4, θ 4–8, α 8–13, β 13–30, γ 30–45 Hz |
| 脑区 | 12（PFC, FL, FR, FC, C, TL, TR, CP, P, PO, O, CB） |
| 归一化 | 每被试每通道 z-score（跨 session 拼接统计量） |

### 4.2 架构

| 参数 | 值 |
|---|---|
| $D_{model}$ / $K_{latent}$ / $D_{pow}$ / $D_{coup}$ | 256 / 32 / 64 / 64 |
| 专家数 $N$ / 社区数 / 记忆维度 | 12 / 4 / 32 |
| 门控温度退火 / 初始偏置 | 0.8 → 3.5（8 轮）/ −0.2 |
| 融合 v6 策略调制器 | $\mathrm{softmax}(\mathrm{proj}_w(O))$；NoMutual 对照 = 均匀权重 |

### 4.3 训练

| 参数 | 值 |
|---|---|
| 优化器 | AdamW（lr=2e-4, weight decay=1e-4） |
| 调度 | CosineAnnealingWarmRestarts（T₀=10） |
| 轮数 / 批大小 | 15 / 64 |
| 种子 | 42 / 123 / 789 |

---

# 第五部分：实验证据链

## 5.0 实验历程与思路优化（完整复盘，简洁版）

**一条主线：从"特征可用"到"机制健康"再到"贡献坐实"。** 每阶段一步实验，一条教训，逐步逼近当前架构。

| 阶段 | 做法 | 结果 | 教训 |
|---|---|---|---|
| **v3 DE协议**（15被试×5种子） | 62通道DE特征 + DAME全家族 + 8基线 | DAME 38.7–39.5%，贴近直接迁移天花板~40%；机制消融全中性 | DE无相位信息，机制无信息可挖；**多种子必须**（1种子正增益是种子噪声） |
| **v3 原始波形·节点级** | RawEncoder节点级DAME | ~33%；机制被调静音（REFLUX_W=PRED_W=0） | **机制必须激活**——调静音后消融中性是循环论证 |
| **v4 脑区耦合** | 62ch→12脑区→PLV耦合，DAME机制挂接 | 耦合 vs 无耦合 **+6.9点**（用户核心前提获数据验证）；但完整版输自己的Base 4.8点 | 机制浮在特征下游→消融中性；预测头与平凡基线打平→不可证伪 |
| **v5 全局更新** | 机制深度绑定耦合：水循环直接蒸馏耦合token、社会=脑区专家+实测PLV邻接初始化、预测头预测耦合本征演化 | 见5.1三轮消融 | 机制必须"长在"耦合上，而非挂在分类器下游 |
| **iter1**（4被试×2种子×8轮） | v5初版 | 30.35；仅耦合为正（−3.3） | 融合用**加法原则**：机制做加法不做替换，消融=删项 |
| **iter2**（5被试×2种子×12轮） | 加法融合+门控看耦合+温度3.5+PRED_W减半 | 30.34；耦合−3.5仍是唯一正机制 | 两轮铁律：耦合唯一硬机制；预测旧接法可靠负贡献；Base压过完整版 |
| **iter3 手术**（8被试×1种子×15轮） | ①ĉ作前瞻特征进融合 ②Z进融合 ③预测损失延迟启动 ④soc_scale 0.5 | **34.18**；耦合−3.7；**水循环+1.6/回流+1.0首次转正**；完整版首次压过Base；但ĉ仍拖累+0.2，selfMSE 0.0002→0.04 | **预测头被分类梯度征用**——ĉ变成了分类辅助特征，预迁移被架空 |
| **iter4 裁决落地**（2026-08-13） | 撤回ĉ；预测目标换**跨trial情绪稳定性**二分类（50%随机基线）；Z保留进融合；Pass2第4轮后启动 | 跑完 3 折（第 4 折被重启中断，硬件收工，见 8.3）：selfMSE=0.0005–0.0008（Z编码耦合✓）、driftCos=0.971–0.979（跨窗口表示稳定✓）、社会激活 0–3/12；**stab=79.7% 已证伪**（= 多数类平凡解或记录 bug，2026-08-14，见 8.3）——完整 8 折 + 修复方案留待实验室显卡 | 预测头与分类器彻底解耦；预测目标必须可证伪；**诊断指标必须逐折落盘 + 平衡配对** |

**思路演进的一句话总结**：特征选对（耦合）→ 机制激活（不调静音）→ 机制绑定（长在耦合上）→ 机制转正（加法融合+解耦预测头）→ 贡献坐实（稳定性目标+统计验证）。

**跨模态支线**：同一架构在NLP（ChnSentiCorp跨域）上取得78.5%，回流+2.08点——机制有效性不依赖模态的旁证。

## 5.1 三轮快速迭代消融（核心表）

协议：iter1 = 4 被试 × 2 种子 × 8 轮；iter2 = 5 被试 × 2 种子 × 12 轮；iter3 = **8 被试 × 1 种子 × 15 轮**。Δ = 删掉该机制后的变化（**负 = 机制有用**）。

| 臂 | iter1 acc | iter2 acc | iter3 acc ± std | iter3 Δ |
|---|---|---|---|---|
| **DAME-C5（完整）** | 30.35 | 30.34 | **34.18 ± 2.63** | — |
| C5-NoCoupling | 27.08 | 26.84 | 30.52 ± 3.86 | **−3.7** ✓ |
| C5-NoWater | 28.29 | 31.62 | 32.59 ± 3.75 | **−1.6** ✓ |
| C5-NoReflux | 30.09 | 31.54 | 33.14 ± 3.87 | **−1.0** ✓ |
| C5-NoMutual | 30.27 | 30.37 | 33.99 ± 3.95 | −0.2 |
| C5-NoPred | 30.91 | 32.58 | 34.37 ± 3.97 | +0.2 |
| C5-Base（纯耦合+线性） | 31.37 | 32.13 | 33.99 ± 4.66 | −0.2 |

**三轮铁律：**

1. **耦合表示是唯一三轮连硬的机制**（−3.3 / −3.5 / −3.7）：脑区联动耦合前提被数据持续验证；
2. **8 被试协议下机制噪声收窄**：水循环（+1.6）、回流（+1.0）首次同时转正；完整版首次不再输 Base（前两轮输 1.0–1.8 点）；
3. **ĉ 进融合拖累**（iter3 +0.2，与 iter1/2 同向）：预测头改分类特征的方向被三轮数据否定，iter4 已撤回并更换为稳定性目标（2.4）。

## 5.2 SOTA 基线对照（同协议）

（基线表：8 被试 × 1 种子 × 15 轮，与 DAME 同预算对比；最终全量将升级为 100 轮 + 早停。2026-08-13 跑完。）

| 模型 | acc（8 被试 LOSO，原始波形） | Δ vs 主配置 | DAME 胜折数 |
|---|---|---|---|
| **DAME-C5（NoPred 主配置）** | **34.37 ± 3.97** | — | — |
| DAME-C5（含 ĉ 融合，已撤回） | 34.18 ± 2.63 | −0.19 | — |
| EEGConformer | 32.55 ± 5.45 | −1.82 | 4/8 |
| DGCNN | 32.12 ± 4.16 | −2.25 | 5/8 |
| DeepCORAL | 31.42 ± 5.55 | −2.95 | 5/8 |
| EEGNet | 30.55 ± 4.84 | −3.82 | 6/8 |
| TSception | 30.24 ± 2.59 | −4.13 | 7/8 |
| LMDA-Net | 29.51 ± 3.12 | −4.86 | 6/8 |
| DANN | 28.13 ± 2.39 | −6.24 | 7/8 |

**结论：DAME-C5 在相同协议（LOSO、SEED-IV 原始波形、同预算 15 轮）下领先全部 7 个 SOTA 基线**（主配置 +1.82 ~ +6.24 点），且在逐折配对比中 4/8 ~ 7/8 折占优。主配置采纳 NoPred 变体（34.37）：三轮一致证据（iter1 Δ+0.6 / iter2 Δ+2.2 / iter3 Δ+0.19）表明预测头 ĉ 前向融合无正贡献，已撤回；预测头转为辅助/教练定位（见 8.2）。DANN 与 DeepCORAL 的域适配在此设置下增益为负（与 NLP 分支 DANN 落后一致：跨被试单数据集内域差不足以支撑对抗对齐，反而破坏判别信号）。该表为快速协议初步证据；统计显著性（配对 t / Wilcoxon / 置换）待最终全量 15 被试 × 3 种子在实验室显卡上补全。

**协议级配置注记（2026-08-14）**：正式全量 15×3 按协议级配置执行——LOSO 主配置 = C5-NoPredNoMutual（纯水循环），跨会话 = C5-NoPred（水+社会），双臂如实呈现（8.2）。8 被试快速证据 NoPred（含社会）34.37 与 NoMutual 33.99 的 +0.38、6 被试×3 种子路由运行 −0.81 的负号，由 5.7 场域条件化命题统一解释，LOSO 社会最终符号待 15×3 裁决。

### 5.2.3 统计显著性方案（最终全量）

15 被试 × 3 种子 = 45 配对样本：配对 t 检验（Nadeau–Bengio 校正，LOSO 标准）+ Wilcoxon 符号秩 + 置换检验稳健性 + 95% CI + Cohen's d；主比较预先声明，其余 Holm–Bonferroni 校正。n=75（5 种子）时检出 ≥1.8 点差距的功效 = 0.8（α=0.05）。

## 5.3 诊断证据（图）

![每类情绪脑区PLV耦合矩阵](results/figures_v5/v5_iter3_DAME-C5_plv_per_class.png)

**图 1 — 每类情绪的脑区 PLV 耦合矩阵**（iter3，6 折平均）：4 类情绪 × 5 频段的 12×12 脑区耦合矩阵，绿框标出 DMN 前后枢纽（PFC ↔ CP）。情感状态在**大尺度耦合模式**上的重组一目了然——这是"脑区联动耦合"前提的直观证据。

![DMN前后枢纽耦合随情绪重组](results/figures_v5/v5_iter3_DAME-C5_dmn_frontback.png)

**图 2 — DMN 前后枢纽（PFC↔CP）耦合随情绪 × 频段重组**：正性/负性情绪在不同频段上对 DMN 前后连接的调制方向不同，与 Bai et al. (2025) 的 θ/α 段 DMN 相位同步发现形成对照。

![门控指纹](results/figures_v5/v5_iter3_DAME-C5_gate_fingerprint.png)

**图 3 — 门控指纹跨折一致性**：12 个脑区专家的门控均值跨折分布。iter3 中部分折已出现专家激活（前两轮全部 0/12），社会开始"活过来"；一致性列 = 策略不动如山的证据。

**图 4 — 社区涌现 → 脑区 → 功能网络分配**：社区分配在多个折中稳定出现 PFC 与后部枢纽（CP/PO/O）分属不同社区的倾向，与 DMN 前后分离结构吻合——互助社会自发涌现出神经科学可对照的功能网络。

![不变性面板](results/figures_v5/invariance_panel.png)

**图 5 — 不变性面板（iter4 半程 3 折，2026-08-14）**：driftCos ≈ 0.97+（跨窗口表示稳定）+ selfMSE ~1e-4（Z 编码耦合本征）——「以不变应万变」的表示层证据。完整面板与红旗记录见 `results/invariance_panel.md`。**注意：iter4 的 stab=79.7% 已被证伪**（stab_acc ≡ 1−stab_prior 逐位互补 + 三折 stab_per_class 逐位相同 → 多数类平凡解或记录 bug，见 8.3），稳定性任务的修复方案（50/50 平衡配对 + 逐折落盘）待全量执行。

## 5.4 跨模态证据（NLP 分支）

同一 DAME 架构在 **ChnSentiCorp 跨域情感分类（NLP）** 上验证：3 种子 × 3 域 LODO，最佳 78.5%，**回流消融 +2.08 点**——水循环/回流机制在文本模态同样有效，架构的模态无关性获得跨模态证据。

## 5.5 定位声明（诚实）

- **直接迁移天花板**：同协议 LOSO + DE 特征的历史工作约 40%（Suo & Li 2026 协议评估）；
- 文献中 62–83% 的 SOTA 多采用 **UDA 协议（目标被试数据参与适配）** 或非 LOSO 协议，与本工作**不可直接对比**；
- 本工作 DAME-C5 原始波形 LOSO = 34.37（NoPred 主配置，8 被试快速协议；含预测融合版 34.18），核心贡献 = **耦合表示（+3.7 点，三轮连硬）+ 水循环蒸馏耦合本征（selfMSE/driftCos 诊断证据）**，而非单纯刷点。稳定性预迁移的信号待全量修复验证（8.3 红旗）。
- 跨会话协议四臂消融：水 +1.61 / 社会 +1.07 / 合体 +2.68 可加（5.6）——场域条件化机制定律 + 部署定理（5.7）构成第二贡献轴：机制贡献的场域边界及其理论解释。

---

## 5.6 跨会话协议：四臂消融（同脑跨试次：单一水 / 单一社会 / 一起）

协议：6 被试 × 3 种子 × 15 轮，train s1+s2 → test s3（全体被试池化），seed 42/123/789。落盘 `results/eeg_v5_xsession3s_results.json` + `results/eeg_v5_xsession_nowater_results.json`（jsonl：v5_xsession3s_verdict / v5_xsession_nowater_verdict，2026-08-14）。

| 臂 | 均值±std | vs Base | 机制含义 |
|---|---|---|---|
| C5-Base（纯耦合+线性） | 29.92±1.25 | — | 底座 |
| C5-NoWaterNoPred | 30.99±1.37 | +1.07 | **单一社会** |
| C5-NoPredNoMutual | 31.85±1.96 | +1.93 | **单一水循环** |
| **C5-NoPred（一起）** | **32.71±1.49** | **+2.79** | **水循环 + 社会** |

**裁决：一起好，且近似可加**——水单独 +1.61、社会单独 +1.07、合体 +2.68（1.61+1.07≈2.68 ✓）。水是主导机制；社会增量（有水在场）+0.86，逐种子 +2.1/+1.4/−0.8（2/3 为正）——快速协议证据，15×3 全量确认后定稿。

## 5.7 场域条件化机制效用的理论解释（Proposition 级，2026-08-16）

**分解模型**：$C_{s,t}(w) = \bar{C}(w) + \delta_s(w) + \varepsilon_{s,t}(w)$ —— 耦合 = 群体共享分量 + 个体特异分量 + 窗级噪声。水循环蒸馏到共享子空间（场域不变载体，两协议均 +1.6 的实证）；社会的边锚定专家 + GRU 记忆拟合的正是 δ 条件下的边动力学——**社会拟合个体特异分量**。

**Proposition 1（场域条件化机制效用）**：设社会拟合的 δ 分量对目标的可迁移性为 κ（源目标特异分量相关性，实测几何 ≈ 会话漂移/跨脑距离），噪声拟合成本 η > 0，则社会对目标误差的贡献 $\Delta_{soc} \propto \kappa - \eta$。推论：同脑跨会话 κ > η → 正（观测 +0.86）；跨被试 δ 近正交、κ → 0 → 负（观测 −0.81）；符号翻转点 κ* = η 是数据集属性，可先用几何探针测量。

**定量一致性（已落盘探针数据，field_fingerprint_probe.py）**：会话漂移 0.126 vs 跨脑距离 0.211 → κ ≈ 0.6；单窗噪声 0.539 vs 脑间信号 0.0006（≈900×）。命题预测的符号对（小域差正 / 大域差负）与观测（+0.86 / −0.81）在方向、对称性、量级上一致。

**Proposition 2（不变性 ⇔ 不可识别）**：若表示 Z 满足场域不变性（P(Z|s) = P(Z|s′)），则任何基于 Z 的场域分类器正确率 ≤ 先验——场域身份无法从不变表示读出；叠加窗级噪声千倍于信号，任何窗级统计量同样不可判。**推论（部署定理）**：机制门控的场域开关必须作为模型外部元数据输入（协议级配置），而非学习对象——与 DA 理论界（Ben-David et al. 2010）以"已知源/目标域对"为条件的设定同构。**跨被试 = 纯水循环、跨会话 = 水+社会 不是工程补丁，是部署定理的必然形态**；"为什么不学一个自动路由器"由 Proposition 2 直接回答。

**诚实注记与先验化验证**：命题在观测到 ±0.86/−0.81 之后提出，几何探针为事后测量——论文按"发现 → 解释"如实呈现。两项先验化验证：(1) **合成仿真**（笔记本可跑，待执行）：显式参数化 δ/ε 的合成耦合数据，扫 κ∈[0,1]，验证 Δ_soc 在 η 处翻转；(2) **新数据集预注册**（DEAP/DREAMER，实验室显卡）：先测几何 → 预注册预测社会符号 → 再跑消融。

---

# 第六部分：代码模块清单

| 模块 | 位置 | 说明 |
|---|---|---|
| 数据加载 | `eeg_v5_coupling_experiment.py: load_raw_with_pairs` | 原始波形 → 窗口 + 跨 trial 配对（session 键修复） |
| 信号处理 | `bandpass_fft / hilbert_analytic / plv_coupling` | FFT 带通 + Hilbert 相位 + PLV（全可微） |
| 前端 | `RegionCouplingV2` | 62ch → 12 脑区 → 耦合/功率双支路 |
| 水循环 | `WaterCycleV2` | VIB 蒸发 + 降雨 + Banach 回流（谱范数收缩） |
| 互助社会 | `MutualSocietyV3` | 边锚定专家（11 入射边×5 频段）+ PLV 邻接初始化 + 策略调制器 + 社区涌现 |
| 稳定性头 | `StabilityPredHead` | 跨 trial 稳定性二分类 + 耦合自洽读出（W_proj 冻结） |
| 训练 | `train_epoch_dame`（两遍）+ `loso_v5` / `session_run_v5`（逐折落盘/断点续跑） | Pass1 全窗口 + Pass2 配对稳定性；session_run_v5 = 跨会话协议（collector 收集预测） |
| 诊断 | `collect_diag` | PLV 矩阵/门控指纹/社区/稳定性三层指标 |
| 分析台 | `analyze_v5_iterations.py` | 迭代进化表（--summary）+ 4 张论文图（--diag） |
| 场域指纹探针 | `field_fingerprint_probe.py` | 4 种指纹 × 判读机制的场域身份可判性穷举（8.2 证伪证据） |
| 场域选择弹窗 | `field_selector_gui.py` | 手动选协议 → 自动分配架构 → 后台双臂运行 → 弹窗输出（已无头测试） |
| 不变性面板 | `invariance_panel.py` → `results/invariance_panel.md` | driftCos / selfMSE / stab 三层诊断 + 红旗记录 |
| 最终裁决 | `final_verdict.py` | 基线表 vs DAME 裁决脚本 |
| 回溯 | `results/v5_iterations.jsonl` + `results/diag_v5/*.npz` | 每轮裁决落盘，全历史可回溯 |

---

# 第七部分：五重哲学根基（EEG 版）

1. **双重表示**——耦合支路（脑区联动动态）vs 活动支路（脑区功率）：相位结构与能量结构互补，融合层两者兼收；
2. **水循环隐喻**——蒸发（VIB 压缩到 32 维本征）→ 降雨（注意力反哺）→ 回流（不动点精炼）：蒸馏去域，压缩≠遗失（$\mathcal{L}_{self}$ 可证伪地保证 Z 保留耦合结构）；
3. **互助社会**——12 脑区专家按实测 PLV 邻接互助、按共激活自发分社区：合作而非对抗，功能网络从数据中涌现而非预设；
4. **预迁移**——预测情绪状态的时序稳定性（保持 vs 翻转）：学到情绪的时序动力学即学到跨被试不变结构；
5. **非线性信息瓶颈**——VIB 蒸馏 + 谱范数收缩（Theorem 1–2）：用可证收敛的压缩替代先验松弛。

**与注意力的本质区别**：注意力是"对输入做加权"；水循环是"把输入蒸馏成本征再反哺"（循环、双向、带收敛定理）；互助社会是"专家间按结构互助"（横向、带涌现）——三者解决的不是同一个问题。

---

# 第八部分：已知局限与改进路线

### 8.1 当前局限

1. **互助社会贡献未转正**（三设计汇总：iter3 加法版 −0.2；v5_mod 节点调制器版 −1.3~−1.6；v5_modedge 边锚定调制器版 −1.86，5/6 折为负——即使专家注视对象对齐耦合边、与基座哲学一致，贡献仍为负）：专家已开始激活，但无论加法槽位、节点锚定调制还是边锚定调制都未转化为分类增益——结论 = 社会调制信号在**跨被试大域差**下不携带分类有用信息；但**跨会话小域差下社会 +0.86 转正且与水循环可加**（5.6）——社会定位 = **场域特异机制**（跨被试负 / 跨会话正，5.7 Proposition 1 统一解释），全量 15×3 两协议各跑双臂如实呈现；
2. **预测头**：ĉ 前向融合设计三轮一致无正贡献（iter1 Δ+0.6 / iter2 Δ+2.2 / iter3 Δ+0.19），已撤回——主配置 = NoPred（34.37）；预测头当前**唯一被证实的用途 = 诊断探针**（selfMSE ~5e-4 证明 Z 编码耦合本征）。iter4 稳定性任务的 79.7% 信号已证伪（= 多数类平凡解或记录 bug，见 8.3），修复方案（50/50 平衡配对 + 逐折落盘 + balanced accuracy）待全量；
3. **种子数**：当前全部为 1 种子（±2.6 的 std 可能低估方差），统计显著性待最终全量；
4. **基线训练预算**：快速协议 15 轮对等预算；最终全量基线需 100 轮 + 早停才能抗"基线没训好"的审稿攻击；
5. **硬件限制**（2026-08-13 用户裁决）：RTX 5070 Laptop 8GB + torch nightly 的驱动 TDR/内核挂起导致基线训练多次中断（已定位为一次性看门狗误杀 + TSception (62,1) conv2d 内核挂起，均已修复）——笔记本算力与稳定性不足以支撑有工程启发价值的进一步迭代。

### 8.2 改进路线（按优先级）

**已落地（2026-08-14，零风险项）**：主配置采纳 C5-NoPred（34.37±3.97，领先最强基线 EEGConformer 1.82 点）；最终裁决落盘 `results/v5_iterations.jsonl`（v5_final_verdict 行，按新标准重裁 = PASS）。**互助社会策略调制器改造 + 两轮快速裁决（同日）**：融合 v6 = `w=softmax(proj_w(O))` 调制各机制项权重（NoMutual 对照 = 均匀权重，结构相同，臂名 C5-NoPredNoMutual）；6 被试×1 种子下 10ep 社会Δ +1.26 / 15ep Δ +1.62（NoMutual−Full，社会为负贡献），15ep 下 Base≈NoPred 说明协议对机制公平、非时长假阴性——**两个设计（加法/调制）均未让社会转正**，社会在论文中定位为探索性可解释模块，全量 15×3 保留双臂如实呈现（jsonl：v5_mod_verdict / v5_mod15_verdict）。**场域选择弹窗工具（同日）**：`field_selector_gui.py`——手动选协议（跨被试 LOSO / 跨会话 s1+s2→s3）→ 自动分配架构（LOSO=纯水循环、跨会话=水+社会）→ 后台双臂运行 + 弹窗输出 acc/机制Δ/混淆矩阵，另存 `results/gui_runs.jsonl`（不污染 v5_iterations.jsonl），已无头测试通过。

**跨会话协议发现（2026-08-14）**：会话内划分（train s1+s2 → test s3，全体被试池化）社会首次转正：NoPred 32.71 vs NoPredNoMutual 31.85（**社会 +0.86**，3 种子 2/3 正）；水循环 +1.61（场域不变，与跨被试 +1.6 一致）；两者完美可加（full +2.68 ≈ 1.61+1.07）。**结论：社会机制场域特异——跨被试（大域差）负贡献，跨会话（小域差）正贡献**；NoWater 消融证实分解干净（jsonl：v5_xsession3s_verdict / v5_xsession_nowater_verdict）。**四臂完整表与裁决（Base 29.92 / 单一社会 30.99 / 单一水 31.85 / 一起 32.71，一起好且可加）见 5.6；理论解释（分解模型 + Proposition 1/2 + κ≈0.6 定量一致性）见 5.7。**

**场域路由器（快慢通路数据驱动自感知，同日证伪收口）**：为让模型无协议标签地自选通路，实现 ω=σ(a(d₀−d)) 场域亲和度路由（慢参考=训练指纹 EMA，快读数=当前样本指纹，d₀=训练 95 分位标定，无泄漏）。三判决门全 FAIL（6 被试×3 种子×15ep）：ω 分离 LOSO 0.49–0.51 vs 跨会话 0.495 无分离；LOSO 社会拖累未消（30.38 vs 31.19）；跨会话 +0.86 反转为 −0.73。离线探针（`field_fingerprint_probe.py`）穷举 4 种指纹×判读机制：区级 PLV 分离度 +0.0034（单窗噪声比脑间信号大近千倍）/ 边级 PLV +0.0088 / 功率最近原型 +z 标定 −0.075（方向反转）/ 功率 margin + 16 窗池化 +0.013——**SEED-IV 窗级统计量不携带可判读的场域身份**（会话漂移 0.126 ≈ 0.7×跨脑距离 0.211）。该证伪同时印证架构前提：耦合量是刻意设计成场域不变的载体，读不出场域身份。收口：`FIELD_ROUTER=False` 默认关闭（ω≡1 恢复 v6 融合），正式全量按协议级配置（跨被试=纯水循环，跨会话=水+社会）（jsonl：v5_router_verdict / v5_router_probe_verdict）。

1. **最终全量**（实验室显卡上执行）：15 被试 × 3 种子 × 15 轮 + 基线 100 轮早停 + 完整统计包（配对 t / Nadeau–Bengio / Wilcoxon / 置换 / CI / Cohen's d / Holm 校正）；
2. **窗口长度扫描（2/4/6s，新）**：PLV 与 DE 对窗长敏感，全量前用 8 被试快扫一遍，可能 ±1~2 点——最低成本的数值杠杆；
3. **iter4 收尾**：稳定性预测头在 8 被试协议下补全 8 折，验证稳定性三层证据；
4. **底座修复（新线索）**：同协议下 v4-Base 35.78 vs v5-Base 33.99，v5 耦合底座低 1.8 点——diff 两者的窗口/耦合计算/分类器细节，若底座回到 35.78 水平，完整版有望 36+；
5. **社会定位定稿 + 先验化验证**：跨被试三种设计（加法/节点调制/边锚定调制）均未转正 → 跨被试 = 纯水循环；跨会话 +0.86 转正且可加 → 跨会话 = 水+社会。场域条件化机制定律 + 部署定理已写入 5.7；两项先验化验证：(1) 合成仿真扫 κ∈[0,1] 验证 Δ_soc 符号翻转（笔记本可跑）；(2) 新数据集（DEAP/DREAMER）先测几何 → 预注册预测社会符号 → 再跑消融；
6. **水循环增强候选**：VIB 直接作用于完整 8 步耦合 token 序列（保留时序动态，而非池化后蒸馏）；
7. **跨数据集泛化（可选）**：SEED-V 同协议复跑，验证耦合机制的可迁移性。

### 8.3 硬件问题排查结论（工程记录）

- **真挂起**仅发生在重启前（nvlddmkm 153 ×4），TSception `Conv2d(48,48,(62,1))` 是唯一确定的挂点；修复 = einsum 等价实现（数值一致，最大绝对差 4.9e-4）；
- **一次性看门狗 bug**：`faulthandler.dump_traceback_later(600, exit=True)` 在进程第 600 秒无条件杀进程，曾造成基线"伪崩溃"误诊；修复 = 训练循环内每 epoch 心跳重 arm，600s 无进度才触发；
- **iter3 nan 插曲（已结案，非数据问题）**：探针证实训练后前向全有限（selfMSE=0.0044）；烟测日志中 nan 行 = Base 臂预期占位；iter3 第 4 折 nan 来自旧 CouplingPredHead（已被 StabilityPredHead 替换）。5.1 表中 iter3 数字为清理后的 8 折全有限数据；
- **未落盘数据注记**：DREAMER 早期 LOSO 仅 TSception 5 折 75.2–78.6% 有记录（Ours/EEGNet 当时未落盘，故不写入正文）；v3 DE 协议全部 11 模型×5 种子明细见 `results/eeg_v3_final.json`；
- 重启后 GPU 无任何新 TDR 事件，全部 7 基线 + EEGNet 已稳定跑完。
- **NoMutual/Base 均值逐位相同（2026-08-14 发现，待核验）**：iter3 两组均值同为 33.99442546069622（float64 逐位一致）但逐折数据不同（std 3.95 vs 4.66）——出现概率 ~10⁻¹⁵，疑为记录层 bug 或极端巧合；**不影响"社会中性"结论**（两组均 33.99，Δ−0.2），待 15×3 全量时一并核验。
- **iter4 stab 信号证伪（2026-08-14）**：stab_acc ≡ 1 − stab_prior（三折 0.797101 vs 0.202899 float64 逐位互补）且三折 stab_per_class 逐位相同——多数类平凡预测（79.7% 恰为稳定对占比，L2 陷阱再现）或 npz 记录 bug；两种解释均推翻"首现强可证伪信号"。落盘 `results/v5_iterations.jsonl`（v5_stab_redflag 行）。修复 = 平衡配对采样 + 逐折独立落盘 + balanced accuracy，全量执行。
- **互助社会策略调制器两轮裁决（2026-08-14）**：v5_mod（10ep）与 v5_mod15（15ep，6 被试×1 种子×seed42）均显示社会存在低于均匀权重对照（Δ +1.26 / +1.62），逐折 3 胜 3 负；15ep 下 Base≈NoPred 排除协议时长假阴性。结论：加法与调制两种设计下社会贡献均未转正，快速协议证据为稳健负号（jsonl v5_mod_verdict / v5_mod15_verdict）。
- **边锚定社会裁决（2026-08-14）**：专家注视对象改为入射耦合边（用户诊断：社会注视元素而非耦合边、与基座违背）——Δ +1.86（5/6 折为负，三设计中最一致的负号）。诊断概念正确但对齐基座后贡献仍未转正 → 问题不在注视对象，社会调制信号在该数据规模下不携带分类有用信息。社会转正线收口，论文定位 = 探索性可解释模块（jsonl v5_modedge_verdict；代码备份 _additive/_modulator，当前文件 = 边锚定版）。
